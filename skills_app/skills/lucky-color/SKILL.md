---
name: lucky-color
description: A skill that  generates lucky number using age and gender
---
If lucky color is requested, run `scripts/find_lucky_color.py` with the `age` and `gender` argument.
Provide the update to the user.
